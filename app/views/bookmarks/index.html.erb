<h1 class="text-center">All Bookmarks</h1>
  <% @bookmarks.each do |bookmark| %>
    <p><%= link_to "#{bookmark.url}", bookmarks_show_path(bookmark) %></p>
  <% end %>
  <% if current_user %>
    <%= link_to "Create Bookmark", bookmarks_new_path, class: 'btn btn-primary btn-large' %>
  <% end %>
